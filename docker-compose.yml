services:
  app:
    image: ${DOCKERHUB_USERNAME}/${APP_NAME}:${IMAGE_TAG}
    restart: unless-stopped
    env_file:
      - .env
    # Note: Application port not specified. Add ports mapping when port is known.
    # Example: ports:
    #   - "5000:5000"