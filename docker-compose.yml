version: '3.8'

services:
  app:
    image: ${DOCKERHUB_USERNAME}/${APP_NAME}:${IMAGE_TAG}
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - app_data:/app/data

volumes:
  app_data: